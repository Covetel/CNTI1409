#Traerme los resultados de todas las urls por Disposición
SELECT j.id,  j.site,  u.path,  re.name,  re.pass FROM jobs j LEFT JOIN urls u
ON j.id = u.pid LEFT JOIN results re ON u.id = re.pid  WHERE re.name =
'Domain';
